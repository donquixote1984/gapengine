
float ShadowCalculation(vec3 norm)
{
    //float dshadow = ShadowCalculationDirectionalLight(norm);
    //float pshadow = ShadowCalculationPointLight(norm);

    //return dshadow + pshadow;
    return 0.0;
}

float ShadowPCF(vec3 norm)
{
    //return ShadowCalculationPointLightPCF(norm);
    return 0.0;
}